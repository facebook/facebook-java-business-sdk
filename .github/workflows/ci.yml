name: Build

on:  []

jobs:
  build:
    runs-on: 

    steps:
      - uses: 
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Build
        run: 
